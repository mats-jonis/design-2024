<div class="header" role="banner">

    <div class="site-header">

        {% if pages["_meta"].meta.logo %}
        <div class="logo">
            <a href="{{ base_url }}" aria-label="Till startsidan">
                <img src="{{ base_url }}/{{ pages["_meta"].meta.logo|url }}" alt="Design v3 logotyp" />
            </a>
        </div>
        {% endif %}

        <div class="title" {{ pages["_meta"].meta.tagline ? ' class="tagline"' }}>
            <a href="{{ base_url }}" aria-label="Till startsidan">
                <h1>{{ site_title }}</h1>
                {{ pages["_meta"].meta.tagline|markdown }}
            </a>
        </div>

    <label for="menu-toggle" class="hamburger" aria-label="Öppna meny">
        <span></span>
        <span></span>
        <span></span>
    </label>
       
    </div>
   

    
    <!-- hamburger menu toggle -->
    <input type="checkbox" id="menu-toggle" class="menu-toggle">
    


     <div class="nav">
        <div id="nav" role="navigation" tabindex="-1">
            <ul>
                {% for page in pages(depthOffset=-1) if page.title and not page.hidden %}
                    <li{% if page.id==current_page.id %} class="active" {% endif %}>
                        <a href="{{ page.url }}">{{ page.title }}</a>
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
</div>
